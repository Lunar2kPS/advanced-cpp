# The final build directory
out

# From the pre-build.sh and post-build.sh scripts
temp-cs-build-instructions.sh

# TODO: Check if this should really be gitignored,
#       It seems to be generated every time the build is run, but is not mandatory for successful startup/running of the build
imgui.ini

# MacOS:
# .DS_Store files are hidden system files that are automatically generated by the Finder.
# Its purpose is to store custom attributes of a folder, such as the position of icons, the choice of background color or image, and other display-related information.
.DS_Store
