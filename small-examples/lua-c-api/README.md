# Lua C API Example
Note that because we use the example.lua script alongside the build exe, it is important to run the build at its directory so relative file paths work correctly during execution of the program. For example,

```console
cd ./out/Debug && ./LuaExample.exe && cd -
```
